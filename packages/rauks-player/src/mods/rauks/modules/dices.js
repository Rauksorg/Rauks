const dices = [dice1]

const dice1 = {
  name: String,
  description: String,
  image: String,
  backgroundColor: String,
  color: String,
  faces: [face1],
  min: Int,
  max: Int
}

const face1 = {
  name: String,
  description: String,
  image: String
}

export default dices
